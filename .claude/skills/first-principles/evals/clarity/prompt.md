---
max_turns: 6
allowed_tools: [Read, Glob, Grep, Skill]
runs: 3
---

Explain what a prompt cache is and why a caching proxy that rewrites a request can silently double what you pay, to someone who writes code but has never worked with a language model API.
